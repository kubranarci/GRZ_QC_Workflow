{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "$id": "https://raw.githubusercontent.com/nf-core/grzqc/master/assets/schema_input.json",
    "title": "BfArM-MVH/GRZ_QC_Workflow pipeline - params.input schema",
    "description": "Schema for the file provided with params.input",
    "type": "array",
    "uniqueEntries": ["sample", "runId"],
    "items": {
        "type": "object",
        "properties": {
            "sample": {
                "type": "string",
                "pattern": "^\\S+$",
                "errorMessage": "Sample ID is required and must not contain spaces.",
                "meta": ["sample"]
            },
            "runId": {
                "type": "string",
                "pattern": "^\\S+$",
                "errorMessage": "runId is required, must be unique per sample, and must not contain spaces.",
                "meta": ["runId"]
            },
            "donorPseudonym": {
                "type": "string",
                "meta": ["donorPseudonym"]
            },
            "labDataName": {
                "type": "string",
                "meta": ["labDataName"]
            },
            "laneId": {
                "type": "string",
                "pattern": "^\\S+$",
                "meta": ["laneId"]
            },
            "flowcellId": {
                "type": "string",
                "meta": ["flowcellId"]
            },
            "libraryType": {
                "type": "string",
                "pattern": "panel|wgs|wes|panel_lr|wgs_lr|wes_lr",
                "errorMessage": "libraryType is required and must be one of: panel, wgs, wes, panel_lr, wgs_lr, wes_lr.",
                "meta": ["libraryType"]
            },
            "sequenceSubtype": {
                "type": "string",
                "pattern": "somatic|germline",
                "errorMessage": "sequenceSubtype is required and must be either 'somatic' or 'germline'.",
                "meta": ["sequenceSubtype"]
            },
            "genomicStudySubtype": {
                "type": "string",
                "pattern": "tumor\\+germline|tumor-only|germline-only",
                "errorMessage": "genomicStudySubtype must be one of: 'tumor+germline', 'tumor-only', or 'germline-only'.",
                "meta": ["genomicStudySubtype"]
            },
            "sequencerManufacturer": {
                "type": "string",
                "errorMessage": "sequencerManufacturer must be a non-empty string naming the sequencing platform manufacturer.",
                "meta": ["sequencer"]
            },
            "reads_long": {
                "type": "string",
                "format": "file-path",
                "exists": true,
                "pattern": "^\\S+\\.(bam|fastq.gz|fq.gz)$",
                "errorMessage": "reads_long must be an existing file path without spaces, ending in '.bam', '.fastq.gz', or '.fq.gz' for long reads."
            },
            "reads1": {
                "type": "string",
                "format": "file-path",
                "exists": true,
                "pattern": "^\\S+\\.f(ast)?q\\.gz$",
                "errorMessage": "reads1 must be an existing FASTQ file path without spaces, ending in '.fastq.gz' or '.fq.gz' for the first read in a pair (R1)."
            },
            "reads2": {
                "type": "string",
                "format": "file-path",
                "exists": true,
                "pattern": "^\\S+\\.f(ast)?q\\.gz$",
                "errorMessage": "reads2 must be an existing FASTQ file path without spaces, ending in '.fastq.gz' or '.fq.gz' for the second read in a pair (R2), and may only be given if reads1 is also provided."
            },
            "aligned_reads": {
                "type": "string",
                "format": "file-path",
                "exists": true,
                "pattern": "\\S+\\.(bam|cram)$",
                "errorMessage": "Path to aligned reads must exist, end in '.bam' or '.cram', and cannot contain spaces. fastp_json must be provided together with aligned_reads."
            },
            "fastp_json": {
                "type": "string",
                "format": "file-path",
                "exists": true,
                "pattern": "\\S+\\.(json)$",
                "errorMessage": "Path to fastp/fastplong JSON report must exist, end in '.json', and cannot contain spaces. It must be provided for runs with aligned_reads.",
                "meta": ["fastp_json"]
            },
            "bed_file": {
                "type": "string",
                "format": "file-path",
                "exists": true,
                "pattern": "^\\S+\\.bed(\\.gz)?$",
                "errorMessage": "Path to target bed file must exist, end in '.bed' or '.bed.gz', and cannot contain spaces.",
                "meta": ["bed_file"]
            },
            "reference": {
                "type": "string",
                "pattern": "GRCh38|GRCh37",
                "errorMessage": "The reference genome has to be either GRCh37 or GRCh38",
                "meta": ["reference"]
            },
            "meanDepthOfCoverage": {
                "type": "number",
                "minimum": 0,
                "errorMessage": "Must be a positive number.",
                "meta": ["meanDepthOfCoverage"]
            },
            "targetedRegionsAboveMinCoverage": {
                "type": "number",
                "minimum": 0,
                "maximum": 1,
                "errorMessage": "targetedRegionsAboveMinCoverage must be a number between 0 and 1.",
                "meta": ["targetedRegionsAboveMinCoverage"]
            },
            "percentBasesAboveQualityThreshold": {
                "type": "number",
                "minimum": 0,
                "maximum": 100,
                "errorMessage": "percentBasesAboveQualityThreshold must be a number between 0 and 100.",
                "meta": ["percentBasesAboveQualityThreshold"]
            },
            "meanDepthOfCoverageRequired": {
                "type": "number",
                "minimum": 0,
                "description": "Required minimum mean depth of coverage for the sample.",
                "errorMessage": "meanDepthOfCoverageRequired must be a positive number.",
                "meta": ["meanDepthOfCoverageRequired"]
            },
            "minCoverage": {
                "type": "number",
                "minimum": 0,
                "description": "Minimum per-base coverage used as threshold for coverage-based metrics.",
                "errorMessage": "minCoverage must be a positive number.",
                "meta": ["minCoverage"]
            },
            "targetedRegionsAboveMinCoverageRequired": {
                "type": "number",
                "minimum": 0,
                "maximum": 1,
                "description": "Required fraction (0–1) of targeted bases at or above minCoverage.",
                "errorMessage": "targetedRegionsAboveMinCoverageRequired must be a number between 0 and 1.",
                "meta": ["targetedRegionsAboveMinCoverageRequired"]
            },
            "qualityThreshold": {
                "type": "integer",
                "minimum": 0,
                "description": "Minimum Phred base quality used to define high-quality bases (typically 20 or 30).",
                "errorMessage": "qualityThreshold is required and must be a positive integer.",
                "meta": ["qualityThreshold"]
            },
            "percentBasesAboveQualityThresholdRequired": {
                "type": "number",
                "minimum": 0,
                "maximum": 100,
                "description": "Required percentage (0–100) of bases with base quality at or above qualityThreshold.",
                "errorMessage": "percentBasesAboveQualityThresholdRequired must be a number between 0 and 100.",
                "meta": ["percentBasesAboveQualityThresholdRequired"]
            },
            "umi_location": {
                "type": "string",                    
                "fa_icon": "fas fa-tape",
                "description": "Location of the UMI(s) to be extracted with fastp.",
                "help_text": "Use if UMIs are not present in the read header, but in a specific location within the reads/fastq header index. This will be used to extract UMIs from reads or index in the fastq header and store them in the RX tag.",
                "enum": ["read1", "read2", "per_read", "index1", "index2", "per_index"],
                "meta": ["umi_location"]
            },
            "umi_length": {                    
                "type": "integer",
                "minimum": 1,
                "fa_icon": "fas fa-tape",
                "description": "Length of the UMI(s) in the read.",
                "help_text": "If UMIs are being extracted using fastp, specify the length of the UMI here. This will be used to extract UMIs from reads and store them in the RX tag.",
                "meta": ["umi_length"]
            },
            "umi_base_skip": {
                "type": "integer",
                "minimum": 0,
                "fa_icon": "fas fa-tape",
                "description": "Number of bases to skip after the UMI(s) in the read when extracting with fastp.",
                "help_text": "If UMIs are being extracted using fastp, specify the number of bases to skip after the UMI here. This will trim some bases after the UMI.",
                "meta": ["umi_base_skip"]
            },
            "umi_in_read_header": {
                "type": "boolean",
                "fa_icon": "fas fa-tape",
                "help_text": "Set to true if UMIs are already present in the header of the read, for instance from using OverrideCycles in bclconvert or umi_tools/extract.",
                "meta": ["umi_in_read_header"]
            }     
        },
        "oneOf": [
            {
                "required": ["reads1"],
                "not": {
                    "required": ["aligned_reads", "reads_long"]
                }
            },
            { "required": ["aligned_reads"], "not": { "required": ["reads1", "reads_long"] } },
            { "required": ["reads_long"], "not": { "required": ["aligned_reads", "reads1"] } }
        ],
        "dependentRequired": {
            "reads1": ["runId"],
            "reads_long": ["runId"],
            "reads2": ["reads1"],
            "fastp_json": ["aligned_reads"],
            "aligned_reads": ["fastp_json"]
        },
        "required": ["sample", "libraryType", "sequenceSubtype", "genomicStudySubtype", "qualityThreshold"]
    }
}
